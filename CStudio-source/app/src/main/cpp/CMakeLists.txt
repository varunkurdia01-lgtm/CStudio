cmake_minimum_required(VERSION 3.22.1)

project("cstudio")

add_library(
        ${CMAKE_PROJECT_NAME}
        SHARED
        native-lib.cpp)

find_library(
        log-lib
        log)

target_link_libraries(
        ${CMAKE_PROJECT_NAME}
        ${log-lib})
